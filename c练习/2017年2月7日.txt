						2017年2月7日
今天我又学到一个关于指针的知识
char *fun(char *p,char *q)
{
	char *y=p;
	while(*y!='\0')
		y++;
	while(*q!='\0')
		*y++=*q++;
	*y='\0';
	return(p);
}					在做类似返回指针函数的题目时候定义一个指针变量当做副本，并把形参中的指针初始化它然后再运算其中的副本或是本体，副本和本体是相关联的，最后返回不参与运算的指针。函数可以调用函数，今天终于结束指针了心情甚好只是还有一堆的课后题
努力吧 少年
